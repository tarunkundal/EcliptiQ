import { Spinner } from '@chakra-ui/react';
import React from 'react';

const LoadingSpinner = () => {
	return <Spinner thickness="4px" speed="0.65s" color="gold" size="lg" />;
};

export default LoadingSpinner;
